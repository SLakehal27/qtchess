#pragma once
#include "Position.hpp"
#include "PieceColor.hpp"

class MoveValidator {
    public:
        bool validateMove(Position position, PieceColor color);
    // private:
        // Chessboard chessboard;
};